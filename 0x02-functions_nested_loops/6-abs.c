#include "main.h"
/**
 * _abs - print the absolute value of a number
 * @n: integer to be checked
 *
 * Return: absolute value of n
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
